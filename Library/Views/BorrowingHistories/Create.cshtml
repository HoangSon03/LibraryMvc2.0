@*@model Library.Models.BorrowingHistory*@
@model Library.ViewModel.HistoryViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>BorrowingHistory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                  <label class="control-label">Borrower :</label>
                <select asp-for="BorrowerId" class ="form-control" asp-items="ViewBag.BorrowerId"></select>
            </div>
             <div class="form-group">
                <label  class="control-label">ListItem :</label>
                <select  asp-for="ListItemId" class ="form-control" asp-items="ViewBag.ItemId" multiple></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
